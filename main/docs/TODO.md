# TODO

-   fastify multipart decorator param for config (= Busboy config - file maxSize etc)
-   @Inject(ENV_VAR) COOKIE_NAME - pattern would be cool
-   Improve multi context support (switchToHttp) in guards, filters etc.
